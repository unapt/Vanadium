{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload",
    "prepare": "pip3 install -r requirements.txt --no-input",
    "build": "pip3 install -r requirements.txt --no-input",
    "start": "uvicorn main:app --reload"
  }
}
